export const PROTECTED_ROUTE_PATTERNS = [
  "/:locale/espace-client(.*)",
  "/:locale/espace-entreprise(.*)",
  "/:locale/client(.*)",
  "/:locale/company(.*)",
  "/:locale/admin(.*)",
  "/:locale/seo(.*)",
] as const;
